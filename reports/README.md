# Test reports (CI)

- **Index of all runs:** [ci/INDEX.md](ci/INDEX.md)
- **Per-run folders:** `ci/run-72/`, `ci/run-73/`, ... (one folder per CI run, never overwritten)
- **Latest pointer:** [ci/LATEST.txt](ci/LATEST.txt)

Local `mvn test` still writes to `test-output/` and `target/surefire-reports/` (gitignored).
